<h1>This is show page : <%= @list.name %></h1>
<ul>
  <% @bookmarks.each do |bookmark| %>
  <li><%= bookmark.movie.title %></li>
  <% end %>
</ul>

<%= cl_image_tag @list.photo.key, height: 300, width: 400, crop: :fill %>
<%= link_to "back", lists_path %>
